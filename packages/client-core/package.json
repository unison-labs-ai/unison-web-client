{
	"name": "@unison/client-core",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"clean": "rm -rf dist .turbo",
		"test": "bun test --path-ignore-patterns '**/*.{db,e2e,live}.test.*'",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@unison/contracts": "workspace:*",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
